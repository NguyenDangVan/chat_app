
var messages_list = $('#chat1');
messages_list.html("<%= j(render 'conversations/conversation_content', user: @user, messages: @messages, conversation: @conversation) %>")



// var conversations = $('#conversations-list');
// var conversation = conversations.find("[data-conversation-id='" + "<%= @conversation.id %>" + "']");

// if(conversation.length !== 1){
//   conversations.append("<%= j(render 'conversations/conversation_content', conversation: @conversation, messages: @messages) %>");
//   conversation = conversations.find("[data-conversation-id='" + "<%= @conversation.id %>" + "']");
// }

// conversation.find('.panel-body').show();

// var messages_list = conversation.find('.messages-list');

// var height = messages_list[0].scrollHeight;
// messages_list.scrollTop(height);

// var messages_list = $('#messages-list');
