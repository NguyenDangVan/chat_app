<div class="title">
  <h1>Start new chat</h1>
  <button type="button" class="btn" data-dismiss="modal" aria-label="close"><i class="material-icons">close</i></button>
</div>
<div class="content">
  <%= form_for [:user, @message], remote: true do |f | %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <div class="form-group">
      <%= f.label :recipient_id %>
      <%= f.text_field :recipient_id, class: "form-control", id: "participant", placeholder: "Add recipient..." %>
    </div>
    <div class="form-group">
      <%= f.label :content do %><% end %>
      <%= f.text_area :content, class: "form-control", id: "message", placeholder: "Send your welcome message..." do %>
        Hmm, are you friendly?
      <% end %>
    </div>
    <%= f.submit "Start new chat", class: "btn button w-100" %>
  <% end %>
</div>
